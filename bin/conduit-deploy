#!/bin/sh
# 生产部署已由 spine 接管（2026-09-07）：serve=前台 compose up，保活者是骨架。
# 旧投影入口保留为路标，防止旧习惯把 restart:no 的 compose 部署到无人看护的状态。
echo "conduit-deploy: 已废止。生产部署改为 spine 质料投影：" >&2
echo "  ~/.spine/runtime/bin/spine publish ~/work/personal/conduit/spine \\" >&2
echo "    --nats nats://100.109.38.77:4222 --node host=rig \\" >&2
echo "    --program-arg serve=--image-tag --program-arg serve=vX.Y.Z" >&2
echo "说明见 spine/material.json 与 AGENTS.md 的生产部署一节。" >&2
exit 2
