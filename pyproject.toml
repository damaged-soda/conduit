[project]
name = "conduit"
version = "0.0.0"
description = "mihomo 配置控制面：订阅 → 可靠 mihomo 配置生成器"
requires-python = ">=3.11"
dependencies = ["pyyaml>=6"]

[project.optional-dependencies]
dev = ["pytest>=8"]

[tool.ruff]
target-version = "py311"
line-length = 100
